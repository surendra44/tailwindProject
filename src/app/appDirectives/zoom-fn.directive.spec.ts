import { ZoomFnDirective } from './zoom-fn.directive';

describe('ZoomFnDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomFnDirective();
    expect(directive).toBeTruthy();
  });
});
